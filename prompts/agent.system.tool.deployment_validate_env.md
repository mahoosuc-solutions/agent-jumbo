### deployment_validate_env

Validate and normalize deployment environment names.

Use before deployment actions to ensure the target is valid.

Accepted environment aliases:

- `production`, `prod`
- `staging`, `stage`
- `development`, `dev`
